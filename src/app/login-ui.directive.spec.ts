import { LoginUIDirective } from './login-ui.directive';

describe('LoginUIDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginUIDirective();
    expect(directive).toBeTruthy();
  });
});
